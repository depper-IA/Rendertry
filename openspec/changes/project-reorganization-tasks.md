# Tasks: Project Reorganization

## 1. Infrastructure
- [ ] 1.1 Create `src/` directory.
- [ ] 1.2 Create `docs/` directory.
- [ ] 1.3 Create `scripts/` directory.

## 2. Migration
- [ ] 2.1 Move `index.html`, `contacto.html`, `nosotros.html`, `rendertry.html` to `src/`.
- [ ] 2.2 Move `css/`, `js/`, `assets/` to `src/`.
- [ ] 2.3 Move `prd.md`, `design.md`, `spec.md` to `docs/`.
- [ ] 2.4 Move `scrape_rims.py` to `scripts/`.

## 3. Design & Branding Update
- [ ] 3.1 Update `docs/design.md` with the new "Racing Red" color palette.
- [ ] 3.2 Update CSS variables in `src/css/index.css` (or equivalent) with new tokens.

## 4. Refactoring (Fixing Links)
- [ ] 4.1 Update internal links in all HTML files in `src/`.
- [ ] 4.2 Update asset paths in CSS files in `src/css/`.
- [ ] 4.3 Update any paths in JS files in `src/js/`.

## 5. Verification
- [ ] 5.1 Verify that `src/index.html` loads with all styles and images.
- [ ] 5.2 Verify navigation between pages works.
- [ ] 5.3 Verify that the new color palette is applied correctly.
